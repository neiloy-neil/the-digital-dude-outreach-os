import { describe, it, expect } from 'vitest';
import { checkSuppression } from './check-suppression';

describe('check-suppression.ts', () => {
  it('should be defined', () => {
    expect(checkSuppression).toBeDefined();
  });
});
